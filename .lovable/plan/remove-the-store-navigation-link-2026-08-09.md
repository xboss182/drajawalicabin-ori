# Remove the "Store" navigation link

## What we'll do
Remove the `Store` link from the desktop navigation in `src/routes/index.tsx` (currently line 194). The `/whatsapp-store` page and admin Store tab remain reachable by direct URL; only the nav entry is removed.

## Out of scope for now
The WhatsApp date/room picker assistant is paused. If you want it later, we can plan it as a separate feature.
